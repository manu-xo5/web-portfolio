---
const { id, class: className } = Astro.props;
---

<dialog
	id={id}
	class={[
		"fixed dialog fcenter z-50 w-full max-w-[calc(100%-2rem)] translate-x-[-50%] translate-y-[-50%] duration-200 sm:max-w-lg rounded-lg bg-transparent overflow-hidden",
		"data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0 data-[state=closed]:zoom-out-95 data-[state=open]:zoom-in-95",
	]}
	onclick="if (event.target === this) this.close()"
>
	<article
		class={[
			"bg-card text-card-foreground p-6 border rounded-lg border-white/10 shadow-lg",
			className,
		]}
	>
		<slot />
	</article>
</dialog>
